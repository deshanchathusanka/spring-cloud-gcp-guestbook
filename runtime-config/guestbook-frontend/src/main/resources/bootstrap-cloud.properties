# Disable the runtime configuration server
spring.cloud.gcp.config.enabled=true

# config name for this profile = frontend_cloud
spring.cloud.gcp.config.name=frontend
spring.cloud.gcp.config.profile=cloud

